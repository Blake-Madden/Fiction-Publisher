\pagestyle{empty}

\begin{center}

\textit{$if(series-header)$$series-header$$else$Also Available$endif$}

  \vspace{1cm}

$for(book1)$
$if(book1.title)$
$book1.title$
$endif$
$endfor$

$for(book2)$
$if(book2.title)$
$book2.title$
$endif$
$endfor$

$for(book3)$
$if(book3.title)$
$book3.title$
$endif$
$endfor$

$for(book4)$
$if(book4.title)$
$book4.title$
$endif$
$endfor$

$for(book5)$
$if(book5.title)$
$book5.title$
$endif$
$endfor$

\end{center}
